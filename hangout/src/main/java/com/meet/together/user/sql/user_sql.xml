<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="user">
	<insert id="USER_SIGNUP" parameterType="user">
		insert into user_info
		(userId, userPwd, userName, userEmail, userPhone, userGender, userCountry)
		values
		(#{userId}, #{userPwd}, #{userName}, #{userEmail}, #{userPhone}, #{userGender}, #{userCountry})
	</insert>
	
	<select id="USER_LOGIN" parameterType="user" resultType="user">
		select usernum,userid, userpwd, username, useremail, userphone, usergender, usercountry
		from user_info
		where 1=1
		<if test="userId != null and userId !=''">
			and userid = #{userId}
		</if>
		<if test="userNum != 0">
			and usernum = #{userNum}
		</if>
	</select>
</mapper>






